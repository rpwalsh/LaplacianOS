/* The workspace AArch64 kernel target injects -Tkernel/linker-aarch64.ld.
   Protected services provide their process layout through ../link.ld; this
   intentionally empty script only neutralizes the inherited workspace path. */
