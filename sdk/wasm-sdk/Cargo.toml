[package]
name = "laplacianos-wasm-sdk"
version = "1.0.0"
edition = "2024"
license = "PolyForm-Noncommercial-1.0.0"
description = "LaplacianOS WASM SDK — WASI shim, capability ABI, .gapp bundle tooling"

[lib]
name = "laplacianos_wasm_sdk"
path = "src/lib.rs"

[dependencies]
